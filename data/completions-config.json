{
  "_comment": "Fill both of these in after you've set up the Google Form + Sheet (see data/README.md for the exact steps). Leave them as empty strings until then -- the sync script will just publish an empty completions list, and the site will hide the 'report your duty' link, rather than failing.",
  "formUrl": "",
  "formResponsesCsvUrl": ""
}
